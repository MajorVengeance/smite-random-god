module.exports = {
  name: 'online',
  description: 'List online users',
  execute(msg, args) {
    // var guild = new Discord.Guild(msg.client);
    // console.log(guild.available);
    // var a = new Array();
    // guild.members.fetch().then(console.log).catch(console.error);
    // a = a.concat(guild.members);
    //var reply = ` ${a.map(q => q.displayName).join(', ')}`;
    // console.log(a);
    msg.reply("done");
  }
}